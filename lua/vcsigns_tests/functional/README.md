# Functional tests
These tests assume that there is a mostly plain nvim setup in the same dir as
your normal nvim config, called `nvim.test`.

For my own setup I only have the `vc*` family of plugins set up pointing to my
local repos. I really should share some minimal config at some point, but for
now I'm probably the only one running these.
